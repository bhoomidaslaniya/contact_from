<?php
$hostname = "localhost";
$username = "root";
$password = "";
$db_name = "contact";
$con = mysqli_connect($hostname, $username, $password, $db_name);
if ($con) {
    // '<script>alert("connection successfully")</script>';
}